services:
  - type: web
    name: ai-datainsight-backend
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: ZHIPU_API_KEY
        sync: false